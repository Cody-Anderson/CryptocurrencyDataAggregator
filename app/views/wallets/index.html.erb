<!DOCTYPE html>
<html>
    <script language="javascript">
      function toggle(s) { 
        var web = document.getElementById(s);
        var displaySetting = s.style.display;
        var webButton = document.getElementById(s + 'Btn');
				
        if (displaySetting == 'block') {
            web.style.display = 'none';
        }
        else { 
          web.style.display = 'block';
        }
      }  
    </script>
    <style>
        div#web {
            display: none;
        }
        div#phone {
            display: none;
        }
        div#pc {
            display: none;
        }
    </style>
    <meta charset="UTF-8">
    <head>
        <title>Wallets</title>
        <link rel="stylesheet" type="text/css" href="/stylesheets/basic_style.css">
    </head>
    <body>
        <%= render "header/header" %>
        <div style="margin: 0 auto; width: 400px;">
            <br>
            <button class="btn" onclick="toggle('web')" id="webBtn">Web</button>
            <button class="btn" onclick="toggle('phone')" id="phoneBtn">Phone</button>
            <button class="btn" onclick="toggle('pc')" id="pcBtn">PC</button>
        </div>
        <div style="margin: 0 auto; width: 400px;">
            <button class="btn" id="web">Web Button Test</button>
            <button class="btn" id="phone">Phone Button Test</button>
            <button class="btn" id="pc">PC Button Test</button>
        </div>
    </body>
</html>